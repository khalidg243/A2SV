class Solution:    
    def findUnion(self, a, b):
        arr_length=len(a)
        for i in range(arr_length):
            if a[i] not in b:
                b.append(a[i])
        return set(b)        
            
