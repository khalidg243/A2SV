class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        nums.sort()
        cnt = 1
        for i in range(len(nums) - 1, -1, -1):
            if nums[i] != nums[i-1]:
                cnt +=1
                if cnt == 3:
                    return nums[i-1]
        return nums[-1]
