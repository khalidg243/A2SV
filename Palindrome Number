class Solution:
    def isPalindrome(self, x: int) -> bool:
            v=""
            y=str(x)
            while y:
                v+=(y[-1])
                y=y[:-1]
            if (v== str(x)):
                return True
            else:
                return False
