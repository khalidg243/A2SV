class Solution:
    def flipAndInvertImage(self, image: List[List[int]]) -> List[List[int]]:
        for arr in image:
            arr.reverse()
            for i in range(len(arr)):                
                if arr[i] == 0:
                    arr[i] = 1
                else:
                    arr[i] = 0
        return image
