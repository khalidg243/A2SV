class Solution:
    def maxWidthOfVerticalArea(self, points: List[List[int]]) -> int:
        points.sort(key = lambda x : x[0])
        largest_seen=0
        for i in range(len(points)-1):
            largest_seen = max(largest_seen, (points[i+1][0]-points[i][0]))
        return largest_seen
